class_name AreaData
extends Resource

# DEPRECATED (Fase Exploration AQW). Substituido por AreaSceneData
# (`scripts/data/area_scene_data.gd`). Esta classe sobrevive como stub
# minimo pra .tres legacy (zones antigas) parsearem. Pode deletar manualmente.

@export var id: StringName = &""
@export var display_name: String = "Area"
