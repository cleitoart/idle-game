# Godot 4+ editor cache, exports, Android build template
.godot/
/android/
export.cfg
export_presets.cfg

# Local Claude Code state (worktrees, plans, memory)
.claude/

# OS / IDE noise
.DS_Store
Thumbs.db
*.swp

# Art workflow sources (heavy binaries — not deliverables)
psd-files/

# Accidental temp/log files dropped at repo root
/cd*.txt
